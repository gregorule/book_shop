#!/bin/bash
export ${DATABASE_URI}
export ${SECRET_KEY}
python3 -m venv venv
. ./venv/bin/activate
pip3 install -r requirements.txt
python3 create.py
python3 -m pytest --cov=app --cov-report term-missing
python3 app.py


